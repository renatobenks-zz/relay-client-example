# Relay schema extensions

It's an example to show how to use relay as client GraphQL
with no GraphQL server, over using the state management to
manage requests also, like redux with saga, or thunk,
or another else.
